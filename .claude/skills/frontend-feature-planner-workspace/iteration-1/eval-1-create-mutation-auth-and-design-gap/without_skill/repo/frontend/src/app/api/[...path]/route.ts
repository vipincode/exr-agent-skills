// Catch-all BFF proxy: forwards every /api/* request to BACKEND_URL.
// Any feature hitting /api/products is already covered — no per-feature route needed.
export {}; // (proxy impl elided in fixture)
