{
  "eval_id": 0,
  "eval_name": "list-binding-happy-path",
  "prompt": "The products grid design is built but it's showing hardcoded samples. Plan how to bind the products feature to our API so it shows real data.",
  "assertions": [
    "Plan file (FEATURE_PLAN) was produced",
    "Records the exact success envelope {success, data, message}",
    "Identifies price is in CENTS (and a format transform)",
    "Cites the API source/rung (monorepo backend source)",
    "Reuse names the axios instance AND the catch-all BFF (no new route)",
    "Uses a TanStack query key namespaced to products",
    "Has a data mapping wiring grid/card to a query hook (replaces SAMPLE)",
    "Includes a Dependencies section",
    "Includes a Testing checklist section"
  ]
}