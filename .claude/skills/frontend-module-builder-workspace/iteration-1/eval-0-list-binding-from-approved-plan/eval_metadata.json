{
  "eval_id": 0,
  "eval_name": "list-binding-from-approved-plan",
  "prompt": "The products grid design is built but it's showing hardcoded samples, and _docs/FEATURE_PLAN_products.md is approved. Build the binding so the grid shows real data from the API.",
  "assertions": [
    "Zod schema mirrors the success envelope (literal-true + data)",
    "Domain types derived via z.infer (no parallel interfaces)",
    "Request fn uses shared axios `api` and unwraps+parses the envelope",
    "useProductsQuery with array key [\"products\", ...]",
    "Template consumes the hook and drops the hardcoded SAMPLE",
    "No redundant per-feature BFF route created (catch-all reused)",
    "Price handled as integer cents (formatting transform)",
    "MODULE_REGISTRY updated to reflect the bound products feature"
  ]
}