{
  "eval_id": 1,
  "eval_name": "vite-mui-swr-formik",
  "prompt": "Onboard this existing React dashboard for the frontend toolkit — establish its contract files so the other frontend skills can work on this codebase.",
  "assertions": [
    "ARCHITECTURE.md records Vite + React 18 + react-router-dom — does NOT prescribe Next.js/App Router",
    "Data fetching recorded as SWR + shared axios apiClient — does NOT prescribe TanStack Query",
    "Styling recorded as MUI + Emotion — does NOT introduce shadcn/Tailwind",
    "Forms recorded as Formik + Yup — does NOT switch the contract to RHF + Zod",
    "Direct-to-backend (no BFF) recorded as reality and surfaced as a finding, not silently rewritten",
    "Folder paradigm recorded as type-grouped (components/ hooks/ pages/), not features/",
    "MODULE_REGISTRY.md seeds Card, PageHeader, LoginForm, useUsers, userSchema/loginSchema, apiClient+fetcher, theme",
    "Non-destructive: no source file modified; .claude/workspace.json has a frontend entry with a vite stack"
  ]
}
