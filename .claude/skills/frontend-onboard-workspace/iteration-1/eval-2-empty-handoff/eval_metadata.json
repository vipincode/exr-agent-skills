{
  "eval_id": 2,
  "eval_name": "empty-handoff",
  "prompt": "Onboard this folder for the frontend toolkit — I want to build the admin UI here.",
  "assertions": [
    "Detects there is no frontend source (express-only scratch package.json) instead of treating it as an existing frontend",
    "Routes to nextjs-bootstrap for scaffolding rather than onboarding in place",
    "Does NOT fabricate a descriptive ARCHITECTURE.md / MODULE_REGISTRY.md for a nonexistent frontend",
    "Does not hallucinate an inventory of components/hooks that don't exist",
    "Pre-existing files (package.json, README.md) untouched",
    "The handoff and what happens next (bootstrap scaffolds + writes the contract itself) is stated clearly"
  ]
}
